#include "main.h"

/**
 * main - _strlen - finds the lenght
 *
 */

int _strlen(char *str)
{
	int a;

	for (a = 0; str[c] != 0; a++)
		;
	return (a);

}

int _strlenc(const char *str)
{
	int a;

	for (a = 0; str[a] != 0; a++)
		;
	return (a);
}
